import React from 'react';
import { shallow } from 'enzyme';
import ActivityIcon from './ActivityIcon'

// czy są potrzebne jakies domyslne propsy
const defaultProps = {}

// powinno zwracac wrapper
const setup = () => { }

describe('ActivityIcon', () => {

    // sprawdz czy element bedzie mial klase fa-check-square
    it.skip('render proper fa for true value', () => { })

    // sprawdz czy element bedzie mial klase fa-ban
    it.skip('render proper fa for false value', () => { })

})
