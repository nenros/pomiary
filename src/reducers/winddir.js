import { FETCH_WINDDIR, FETCHING } from "../contants";

const winddir = (state = {}, action) => {
    const { type, action: { id, data } = {} } = action
    switch (type) {
        default:
            return state;
    }
}

export default winddir
