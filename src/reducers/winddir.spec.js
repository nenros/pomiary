import winddir from './winddir'
import { FETCH_WINDDIR, FETCHING, FETCH_WINDDIR_ERROR, FETCH_WINDDIR_SUCCESS } from "../contants";

describe.skip('Winddir reducer', () => {
    it("should render initial state with empty object")
    it(`should set ${FETCHING} for proper id for action ${FETCH_WINDDIR}`)
    it(`should set error for id for action ${FETCH_WINDDIR_ERROR}`)
    it(`should set data for if for action ${FETCH_WINDDIR_SUCCESS}`)

});
